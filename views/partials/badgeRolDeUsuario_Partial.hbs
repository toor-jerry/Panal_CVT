{{#if (ifeq userRole "USER_ENTERPRISE")}}<span class="badge badge-pill badge-primary">Empresa</span> {{/if}}
                    {{#if (ifeq userRole "USER_PERSONAL")}}<span class="badge badge-pill badge-success">Estudiante</span> {{/if}}
                    {{#if (ifeq userRole "USER_ADMIN")}}<span class="badge badge-pill badge-warning">Administrador</span> {{/if}}
                    {{#if (ifeq userRole "SUPER_USER")}}<span class="badge badge-pill badge-danger">Súper Administrador</span> {{/if}}